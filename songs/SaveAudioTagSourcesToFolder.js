(async () => { for (const audio of document.querySelectorAll('audio')) if (audio.src) { const filename = decodeURIComponent(new URL(audio.src).pathname.split('/').pop()); if (filename.toLowerCase().includes('full')) continue; const blob = await (await fetch(audio.src)).blob(), link = document.createElement('a'); link.href = URL.createObjectURL(blob); link.download = filename; link.click(); setTimeout(() => URL.revokeObjectURL(link.href), 1000) } })()